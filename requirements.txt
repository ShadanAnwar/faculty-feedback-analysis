langchain_groq==0.0.1  # Adjust the version accordingly if needed
langchain_core==0.0.1  # Adjust the version accordingly if needed
pandas==1.5.3
smtplib  # Default library for SMTP, no need to add in requirements, but you can install email-related packages like email-validator if needed
matplotlib==3.7.1
fpdf==1.7.2
streamlit==1.24.0
textblob==0.17.1
nltk==3.8.1
